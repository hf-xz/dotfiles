- defaults:
    link:
        create: true
        relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.alias:
    ~/.tmux.conf:
    ~/.config/nvim: nvim/
    ~/.vimrc: nvim/vimrc

- create:
    - ~/.cache/nvim/backup
    - ~/.cache/nvim/undo
    - ~/.cache/nvim/swap

- shell: []
